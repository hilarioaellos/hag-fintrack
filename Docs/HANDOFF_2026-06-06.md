# Handoff — Sesión 2026-06-06

## Estado general

Plan v1.4 completado al 100%. Se inició el backlog y se añadió HAG-64 (feature nueva).

---

## Issues completados en esta sesión

### HAG-62 — A2: Presupuesto estimado desde histórico ✅
**Backend** (`a763873`): `historyEstimate` query + `applyHistoryEstimate` mutation en `budgets.ts`.
- Filtra por `defaultCurrency` del usuario y aplica exclusiones `forceExclude`/`excludeFromReports`
- Retorna `{ currencyCode, rows }` — nunca mezcla monedas
- `averageCents = total / lookbackMonths` (divisor fijo — estimación conservadora)

**Frontend** (`7465da0`): `HistoryEstimateDialog` con lookback 3/6/12 meses, checkboxes por categoría, overwrite toggle, resultado inline. Botón "Estimate from history" en `BudgetList`.

---

### HAG-63 — A5: Gastos compartidos ✅
**Backend** (`55b176b`): `createShared` mutation en `transactions.ts`.
- Crea transacción de gasto + acreencia atómicamente en un solo step
- `currencyCode` derivado de `account.currencyCode` — NUNCA aceptado del cliente
- `sharedAmountCents` debe ser entero > 0 y < `amountCents`

**Frontend** (`b534648`): Toggle "Split this expense" en `TransactionFormDialog` (solo en expense create). Campos `debtorName` + `sharedAmount`.

---

### HAG-49 — ESLint: circular JSON crash ✅ (`a7e4749`)
- `eslint.config.mjs` reescrito con flat config nativo de `eslint-config-next` sin `FlatCompat`
- `scripts/` y `src/convex-generated/` excluidos del lint
- `react-compiler/react-compiler`, `react-hooks/purity`, `react-hooks/set-state-in-effect` deshabilitados — proyecto NO usa React Compiler
- `CSVImportDialog.tsx`: imports no usados eliminados

---

### HAG-44 — currencyCode validación ISO 4217 ✅
**Backend** (`4b65ff2`): `validateCurrencyCode()` movido a `_money.ts` como helper compartido. Usado en `accounts.ts`, `receivables.ts`. Previene insertar monedas inválidas.

**Frontend** (`5f333d9`): Validación en `AccountFormDialog` antes del submit (regex 3 letras + Intl.NumberFormat). Error inmediato al usuario.

**Crash fix** (`4464fff`): `formatMoney()` en `money.ts` tiene try-catch — nunca lanza `RangeError` en render aunque llegue un código inválido de la BD.

---

### HAG-50 — Reports: loading state muestra no-data ✅ (`2f9efb8`)
Los 3 charts (`IncomeExpensesChart`, `CategoryPieChart`, `CashFlowChart`) ahora distinguen:
- `data === undefined` → skeleton animado (`animate-pulse`)
- `data.length === 0` → mensaje "noData"

Antes: `data ?? []` trataba loading y vacío igual.

---

### HAG-64 — Selector de moneda en Reports y Dashboard ✅
**Backend** (`435822e`):
- `accounts.getDistinctCurrencies` — devuelve monedas únicas de cuentas activas
- `accounts.netWorthCents` — acepta `currencyCode?` opcional
- `reports.incomeVsExpenses`, `expensesByCategory`, `cashFlowByDay`, `netWorthSnapshot` — aceptan `currencyCode?`
- `transactions.monthlyStats` — acepta `currencyCode?`
- Si no se provee, todas leen `defaultCurrency` de `fintrack_user_settings` (backward compatible)

**Frontend** (`a99810f`):
- Nuevo `CurrencySelector.tsx` — dropdown que recibe array de monedas del padre; retorna null si ≤1 moneda; skeleton si `currencies === undefined`
- `ReportShell.tsx` — resuelve `effectiveCurrency`: selección local → defaultCurrency si activa → primera activa → "USD"
- Los 4 charts de reportes reciben `currencyCode` prop
- `WidgetGrid.tsx` — mismo patrón para dashboard
- `StatCard.tsx` — recibe `currency` prop y pasa a `formatMoney`

---

## Arquitectura relevante

### Convex deployment
- Dev: `focused-swan-416` → `https://focused-swan-416.convex.cloud` (Railway apunta aquí)
- Prod: `wandering-herring-170` → `https://wandering-herring-170.convex.cloud` (NO activado en Railway aún)
- Para sincronizar cambios al dev que Railway usa: `npx convex dev` en `Landingpage-HAG-Partner` → esperar `✓ Convex functions ready` → `Ctrl+C`

### Linear
- API Key: guardada en `.env.local` (no exponer en docs)
- State IDs: Done=`f1bb0bc3-ff59-4d58-809e-12f12d90c619`, InProgress=`2cdd63c0-4c7f-4f06-8a1e-f8a541aaa6d7`, Todo=`74d5ba1e-0fdf-4cb9-8db2-1bd9d2423710`

### Patrones clave
- `validateCurrencyCode()` en `_money.ts` — helper compartido para todos los módulos backend
- `formatMoney(cents, currency)` — siempre pasar `currency` explícito, nunca depender del default USD
- `CurrencySelector` — recibe `currencies: string[] | undefined` del padre (no hace queries propio)

---

## Backlog — completado en sesión continuación 2026-06-06

### HAG-53 — Snowball warning multi-moneda ✅
- Warning banner en `DebtsList.tsx` cuando `currencies.size > 1 && strategy === "snowball"`
- `validateCurrencyCode` en `debts.ts` migrado al helper compartido de `_money.ts`
- Commits: `335abac` (frontend), `2620913` (backend)

### HAG-51 — Reports: $ hardcodeado → Intl currency ✅
- Nueva `formatMoneyCompact(cents, currency)` en `src/lib/money.ts` (Intl compact con try-catch)
- Los 3 charts usan `formatMoney`/`formatMoneyCompact` con `currencyCode` prop
- `CashFlowChart.labelFormatter` usa `t("day", { day })` — nueva clave i18n `reports.day`
- YAxis width 40→56 para símbolos largos (MX$)
- Commit: `dad29a7`

### HAG-52 — Debts: validation errors i18n ✅
- 9 `setError("literal en inglés")` → `setError(t("errorKey"))` en `DebtFormDialog.tsx`
- 9 claves nuevas bajo `debts` en en/es.json
- Commit: `f91cd88`

### HAG-54 — Reconciliation: i18n incompleto ✅
- 6 strings hardcodeados: 2 errores validación, botón "New reconciliation", "(optional)", fecha date-fns, "Bank:"
- `date-fns` eliminado de `ReconciliationHistory` → `Intl.DateTimeFormat(locale, ...)`
- 5 claves nuevas bajo `reconciliation` en en/es.json
- Commit: `ff00a5a`

### HAG-47 + HAG-48 — Notifications i18n ✅
- Backend: `message: account.name` (solo dato dinámico — no el string inglés completo)
- Frontend: `n.type === "payment_due" ? t("paymentDue", { account: n.message }) : n.message`
- `aria-label` → `t("ariaLabel")`, severity badge → `t("severity.*")`
- Claves nuevas: `ariaLabel`, `paymentDue`, `severity.urgent/warning/info`
- Commits: `6a3dd01` (frontend), `bbb66f9` (backend)

### HAG-43 — CSV Import: DD/MM/YYYY ✅
- `normalizeDate(raw, format)` convierte DD/MM/YYYY o MM/DD/YYYY → YYYY-MM-DD antes de enviar al backend
- Selector "Date format" en paso "map": MM/DD/YYYY (US, default), DD/MM/YYYY (Europe/LATAM), YYYY-MM-DD (ISO)
- Backend `parseDateSafe` sin cambios — siempre recibe ISO del frontend
- Commits: `6ede785` (frontend), `dd8eee8` (backend comment)

### HAG-46 — CSV Import: partial import state ✅
- `batchImport` captura errores por batch con try/catch → retorna `{ ..., partialError? }` en lugar de lanzar
- Frontend "done" step: `AlertTriangle` + "Partial import" + banner rojo + nota de re-import seguro
- Re-import siempre es seguro: `importHash` deduplication salta filas ya importadas
- Commits: `0de9405` (frontend), `28b19a6` (backend)

### HAG-45 — SkippedRow tipo TypeScript ✅
- `type SkippedRow` movido de dentro del componente a nivel módulo en `CSVImportDialog.tsx`
- `type SkippedRow` declarado a nivel módulo en `import.ts`; 2 arrays anónimos → `SkippedRow[]`
- Commits: `eb4a20f` (frontend), `48bd43b` (backend)

---

## Estado final

**Backlog 100% completo.** No hay issues pendientes conocidos.

### Pendiente operacional
- [x] `npx convex dev --once` en `Landingpage-HAG-Partner` ejecutado 2026-06-06 — "✓ Convex functions ready (1.45s)" — deployment dev `focused-swan-416` sincronizado con todos los cambios de ambas sesiones
- [ ] Cuando se decida migrar a prod Convex: actualizar `NEXT_PUBLIC_CONVEX_URL` en Railway a `https://wandering-herring-170.convex.cloud` y correr `npx convex deploy`
