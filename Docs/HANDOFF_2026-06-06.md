# Handoff — Sesión 2026-06-06 (QA + Bugfixes)

## Estado actual

- **v1.4 completo** — todos los issues de backlog cerrados en Linear
- **Landing page** — tab FinTrack activa en hagpartner.com/private (`NEXT_PUBLIC_FINTRACK_URL` configurada en Railway Landingpage-HAG-Partner)
- **QA en progreso** — TEST_DATA_GUIDE ejecutándose, varios bugs corregidos durante la sesión
- **Fase 1:** dev Convex `focused-swan-416` es el backend de producción actual

---

## Bugs corregidos en esta sesión

| Commit | Fix |
|---|---|
| `95acb91` | Sign-out blank screen — `window.location.href` en lugar de `router.push` (race condition con middleware Convex auth) |
| `ec27876` | TransactionFormDialog — labels explícitos en SelectValue account/category; `key={editTx?._id}` para reset de estado al editar distinta transacción |
| `c3a2b88` | Budget — badge de moneda (MXN/USD) en header; fix SelectValue en BudgetFormDialog (categoría mostraba ID Convex) |
| `388820c` | Formularios — currency auto-deriva de cuenta en Subscription (create only); Debt/Receivable usan Select de monedas del usuario en lugar de text input; `useEffect` con `open` en dep array para correcta inicialización |
| `f08219a` | CSV Import + Reconciliation — SelectValue explícito en account selectors; column selectors muestran nombre del header en lugar del índice numérico |

---

## Archivos de QA creados

| Archivo | Descripción |
|---|---|
| `Docs/TEST_DATA_GUIDE.md` | Guía paso a paso para crear datos de prueba (10 módulos) |
| `Docs/QA_CHECKLIST.md` | 50+ items con [SETUP]/[VERIFY]/[EDGE] por módulo |
| `Docs/test-import-bbva.csv` | CSV de prueba con 22 transacciones en DD/MM/YYYY para probar import |

---

## Hallazgos QA — estado por módulo

| Módulo | Estado |
|---|---|
| Landing Page tab FinTrack | ✅ Funciona (Railway redesplegado con var correcta) |
| Sign-out | ✅ Corregido |
| Transactions — form | ✅ Corregido (SelectValue + stale state) |
| Budget — estimate from history | ✅ Funciona (requiere defaultCurrency = MXN en Settings y transacciones con categoría) |
| Budget — currency badge | ✅ Agregado |
| Budget — add manual category | ✅ Corregido (SelectValue) |
| Subscriptions — form | ✅ Corregido (currency auto-derive, icons) |
| Debts — currency | ✅ Corregido (Select de monedas del usuario) |
| Receivables — currency | ✅ Corregido (Select de monedas del usuario) |
| CSV Import | ✅ Corregido (SelectValue account + column headers) |
| Reconciliation | ✅ Corregido (SelectValue account) |
| Cards tab | ⚠️ Skipped — ver backlog futuro |
| Reports, Notifications, i18n | 🔲 Pendiente de ejecutar |

---

## Backlog futuro (sin Linear issue aún)

1. **Fusionar Cards en AccountFormDialog** — cuando type=Credit, mostrar campos: closingDay, paymentDueDay, creditLimitCents, minimumPaymentCents. Eliminar tab Cards del nav. Al guardar, crear/actualizar registro `fintrack_credit_cards` vinculado.

2. **Categorías personalizadas** — gestor para crear, renombrar, agrupar subcategorías, asignar iconos propios.

---

## Fases pendientes hacia producción

### Fase 2 — Usuarios reales en dev (tras QA limpio)
- Completar QA_CHECKLIST sin items fallidos
- Limpiar datos de prueba (TEST_DATA_GUIDE → sección "Limpieza post-QA")
- Invitar 2 usuarios reales
- Observar 1-2 semanas con uso real

### Fase 3 — Migración a Convex prod
Pre-checklist antes del switch:
- [ ] `NEXT_PUBLIC_CONVEX_URL` en Railway hag-fintrack → apuntar a `wandering-herring-170`
- [ ] Auth callback URLs configuradas en Convex prod
- [ ] `NEXT_PUBLIC_FINTRACK_URL` en Railway Landingpage-HAG-Partner sigue siendo `https://fintrack.hagpartner.com` (no cambia)

Pasos:
1. `npx convex deploy` en Landingpage-HAG-Partner → publica a `wandering-herring-170`
2. Actualizar `NEXT_PUBLIC_CONVEX_URL` en Railway hag-fintrack
3. Redeploy → verificar que auth funciona
4. Re-ingresar datos manualmente (Convex no migra entre deployments)

---

## Rutas clave

| Recurso | URL / Path |
|---|---|
| FinTrack prod | https://fintrack.hagpartner.com |
| Portal privado | https://hagpartner.com/private |
| Railway hag-fintrack | railway.app → proyecto hag-fintrack |
| Railway Landingpage | railway.app → proyecto Landingpage-HAG-Partner |
| Convex dev dashboard | https://dashboard.convex.dev → focused-swan-416 |
